.layout-footer
  @extend $grid
  border-spacing unitz()
  background off-white
  color gray-dark
  @media max-small
    border-spacing unitz(0.5) unitz()

.layout-footer-column
  vertical-align middle
  @extend $vertical_spacing
  @media max-small
    display block
    text-align center
  &.sub-right
    text-align right
    @media max-small
      text-align center

  iframe
    position relative
    top 4px
    width 50px